# Kamal loads this file at deploy time. Values are pulled from .env.production
# (gitignored) so no secret is ever committed. Only the registry password is
# needed for a static site.
KAMAL_REGISTRY_PASSWORD=$(grep -m1 '^KAMAL_REGISTRY_PASSWORD=' .env.production | cut -d= -f2-)
